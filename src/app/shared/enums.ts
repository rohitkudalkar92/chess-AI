export enum Variant {
  White = 'white',
  Black = 'black',
}

export enum ButtonSize {
  Sm = 'sm',
  Md = 'md',
  Lg = 'lg',
}

export enum TooltipPosition {
  Top = 'top',
  Bottom = 'bottom',
  Left = 'left',
  Right = 'right',
}

export enum TooltipTrigger {
  Hover = 'hover',
  Focus = 'focus',
  Both = 'both',
}
